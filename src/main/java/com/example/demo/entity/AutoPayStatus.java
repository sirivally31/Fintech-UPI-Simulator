package com.example.demo.entity;

/**
 * Enum representing execution lifecycle status for AutoPay mandates.
 */
public enum AutoPayStatus {
    ACTIVE,
    PAUSED,
    CANCELLED,
    FAILED,
    COMPLETED
}
